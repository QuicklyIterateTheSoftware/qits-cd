# Test-only overrides. This module is the deployable, so its app-level settings live in
# src/main/resources/application.properties and Quarkus MERGES that file into the test config — it is
# not shadowed by this one. Never re-declare an app-level setting here: a test copy that drifts from
# the shipped copy makes the suite lie, passing on a value no deployment ever gets.
#
# What belongs here is only what a test run genuinely needs to be different: the in-memory database.
# The docker seam is not config at all — the suite installs FakeDeploymentDriver (@Mock), so no test
# ever reaches the docker CLI.

# The service suite runs against in-memory H2 (no files under ~), overriding the file-H2 default
# shipped in the cd jar's META-INF/microprofile-config.properties. clean-at-start wipes between runs.
quarkus.datasource.cd.db-kind=h2
quarkus.datasource.cd.jdbc.url=jdbc:h2:mem:cd-svc;DB_CLOSE_DELAY=-1
quarkus.hibernate-orm.cd.datasource=cd
quarkus.hibernate-orm.cd.packages=eu.wohlben.qits.cd.entity
quarkus.flyway.cd.migrate-at-start=true
quarkus.flyway.cd.clean-at-start=true
quarkus.flyway.cd.locations=classpath:db/cd/migration
